# Supabase project backing the watchlist feature (project ref: zlelldmqrrcfitqwwhri).
# The service role key bypasses RLS — server-side only, never expose it to the
# client or commit a real .env.
SUPABASE_URL=https://zlelldmqrrcfitqwwhri.supabase.co
# Dashboard → Project Settings → API keys → service_role
SUPABASE_SERVICE_ROLE_KEY=

